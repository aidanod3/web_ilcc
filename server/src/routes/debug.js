/*
 * routes/debug.js — WebSocket handler for the debug workflow.
 * TODO: implement when debug mode is ready.
 */

const express = require('express');
const router  = express.Router();

module.exports = router;
